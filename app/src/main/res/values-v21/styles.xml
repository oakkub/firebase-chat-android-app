<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:colorEdgeEffect">@color/gray</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowEnterTransition">@transition/content_no_fade</item>
        <item name="android:windowExitTransition">@transition/content_no_fade</item>

        <item name="android:windowSharedElementEnterTransition">
            @transition/shared_element_transition
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/shared_element_transition
        </item>
    </style>

    <style name="SmallProgressBarStyle" parent="@android:style/Widget.Material.ProgressBar.Small" />

    <style name="MediumProgressBarStyle" parent="@android:style/Widget.Material.ProgressBar.Small">
        <item name="android:minWidth">@dimen/spacing_big</item>
        <item name="android:maxWidth">@dimen/spacing_big</item>
        <item name="android:minHeight">@dimen/spacing_big</item>
        <item name="android:maxHeight">@dimen/spacing_big</item>
    </style>

    <style name="AppTheme.NoActionBar.Launcher">
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@drawable/window_background</item>
    </style>

</resources>
